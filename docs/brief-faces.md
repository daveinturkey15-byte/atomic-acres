# Task brief: faces

Read `docs/SPARK-CONTEXT.md`, then **`docs/brief-impl.md`** (the wave rules — they bind
you), then **`docs/REAL-REFERENCE.md`** in full.

## Files you own exclusively

src/build/orange-house.ts and src/build/white-house.ts

Nothing else. Other agents own `src/core/materials.ts`, `src/core/world.ts`,
`src/core/post.ts`, `src/core/renderer.ts`, `src/main.ts`, `src/weapons/`,
`src/ui/`, `index.html` and `scripts/blender/` right now.
`src/core/palette.ts` is READ-ONLY — the keys you need are already in it.

## Your task

REAL-REFERENCE items 7 and 8 - the garage FACES from the street (open canopy as a flat translucent slab on columns, a Welcome banner, ribbed sectional doors with one standing open showing shelving, a door number, cabinets) and DRESSED INTERIORS, doing the two signature rooms first: a teal-grey living/dining plus kitchen, and a purple-diamond and gold bedroom with mustard bunks and a mint ensuite. Also the orange-stripe prefab utility cell with its wall station and plaques where the footage actually shows them, and nowhere else

Open the frames `REAL-REFERENCE.md` cites for your items and look at them before you
build. Follow the gates in `docs/brief-impl.md` and report what you actually saw
change in your captures.
