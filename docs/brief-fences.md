# Task brief: fences

Read `docs/SPARK-CONTEXT.md`, then **`docs/brief-impl.md`** (the wave rules — they bind
you), then **`docs/REAL-REFERENCE.md`** in full.

## Files you own exclusively

src/build/yards.ts and src/build/surround.ts

Nothing else. Other agents own `src/core/materials.ts`, `src/core/world.ts`,
`src/core/post.ts`, `src/core/renderer.ts`, `src/main.ts`, `src/weapons/`,
`src/ui/`, `index.html` and `scripts/blender/` right now.
`src/core/palette.ts` is READ-ONLY — the keys you need are already in it.

## Your task

REAL-REFERENCE items 1 (fences: two builds on a stone plinth - low front ~1.2m vertical boards with a scalloped top rail on 0.3m base, tall side/rear ~1.8-2.0m horizontal stacked boards on a 0.35-0.6m rubble plinth) and 10 (yard and street clutter as DESIGNED COVER at 3-4m gaps, not even scatter: shelter mound, trefoil sign, Security board, bins, DO NOT STACK crates, trash cans, turf rolls, planters, barrier-plus-plaque clusters, hedges at 1.2-1.4m, a yellow mailbox on a pier, stepping discs, hydrant, vents)

Open the frames `REAL-REFERENCE.md` cites for your items and look at them before you
build. Follow the gates in `docs/brief-impl.md` and report what you actually saw
change in your captures.
